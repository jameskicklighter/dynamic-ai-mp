DAI_construction_decisions = {
	DAI_JAP_queue_niigata_land_facility = {
		allowed = { original_tag = JAP }
		visible = {
			is_ai = yes
			NOT = { date < 1937.7.1 }
		}
		available = {
			land_facility < 1
			NOT = { any_controlled_state = { land_facility > 0 } }
			534 = {
				is_owned_and_controlled_by = ROOT
				can_construct_building = land_facility
				free_building_slots = { building = land_facility province = 1117 size > 0 }
			}
		}
		cost = 0
		days_re_enable = 7
		ai_will_do = { base = 999 }
		complete_effect = {
			534 = {
				add_building_construction = { type = land_facility province = 1117 level = 1 instant_build = no }
			}
		}
	}

	DAI_SOV_queue_kazan_land_facility = {
		allowed = { original_tag = SOV }
		visible = {
			is_ai = yes
			NOT = { date < 1938.1.1 }
		}
		available = {
			land_facility < 1
			NOT = { any_controlled_state = { land_facility > 0 } }
			249 = {
				is_owned_and_controlled_by = ROOT
				can_construct_building = land_facility
				free_building_slots = { building = land_facility province = 6338 size > 0 }
			}
		}
		cost = 0
		days_re_enable = 7
		ai_will_do = { base = 999 }
		complete_effect = {
			249 = {
				add_building_construction = { type = land_facility province = 6338 level = 1 instant_build = no }
			}
		}
	}

	DAI_JAP_queue_east_hebei_air_base = {
		allowed = { original_tag = JAP }
		visible = {
			is_ai = yes
			date > 1936.7.1
		}
		available = {
			609 = {
				is_controlled_by = ROOT
				air_base < 3
				can_construct_building = air_base
				# Air bases cap at ten; reserve seven slots to stop at level three.
				free_building_slots = { building = air_base size > 7 }
			}
		}
		cost = 0
		days_re_enable = 7
		ai_will_do = { base = 999 }
		complete_effect = {
			609 = {
				# Queue the missing levels together; native free slots include queued levels.
				if = {
					limit = { free_building_slots = { building = air_base size > 9 } }
					add_building_construction = { type = air_base level = 3 instant_build = no }
				}
				else_if = {
					limit = { free_building_slots = { building = air_base size > 8 } }
					add_building_construction = { type = air_base level = 2 instant_build = no }
				}
				else_if = {
					limit = { free_building_slots = { building = air_base size > 7 } }
					add_building_construction = { type = air_base level = 1 instant_build = no }
				}
			}
		}
	}
}
